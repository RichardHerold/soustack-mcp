{
  "name": "@soustack/mcp",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "soustack-mcp": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc && cp dist/server.js dist/server",
    "dev": "ts-node src/cli.ts",
    "smoke": "node scripts/smoke.mjs",
    "test": "npm run build && NODE_OPTIONS=\"--require ./test/soustack-require.cjs\" node --test --loader ./test/soustack-loader.js test/*.test.*"
  },
  "dependencies": {
    "soustack": "^0.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.19",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.2"
  }
}
